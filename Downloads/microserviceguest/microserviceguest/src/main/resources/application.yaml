server:
  port: 8082

spring:
  application:
    name: microserviceguest

  datasource:
    url: jdbc:mysql://localhost:3306/guest_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true