server:
  port: 8083

spring:
  application:
    name: microserviceregistration

  datasource:
    url: jdbc:mysql://localhost:3306/registration_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true