server:
  port: 8085

spring:
  application:
    name: microservicelogistics

  datasource:
    url: jdbc:mysql://localhost:3306/logistics_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true